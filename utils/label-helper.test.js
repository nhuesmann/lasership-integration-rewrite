const expect = require('chai').expect;

const testDirectory = `${__dirname}/../test`;
const seed = require(`${testDirectory}/seed.js`);
const labelHelper = require('./label-helper.js');

describe('The label helper module', function () {
  // before();

  it('saveLabelAndTracking() should save label to disk and return tracking number', function () {

  });

  it('mergeLabels() should merge all shipping labels into a single PDF', function () {

  });

  it('archiveLabels() should move all individual PDFs to a new archive folder', function () {

  });

  // after();
});
